-- Jafar developper TODO list --
--------------------------------

* jafar-module 
  
  - jafar-module -u (update des fichiers non modifiables avec les
templates), verif repertoire existe
  - parser les options avec un while {} pour pouvoir faire des
combinaisons d'options.

* makefile

  - le "make install" installe aussi les *.hpp~ de xemacs...
  - installe la doc des modules dans JAFAR_DIR/doc/doxygen
  - ajout d'un macro/pkg pour chaque module, les .tcl de macro/pkg
seraient ajoute dans le package tcl.

* mode emacs

 - mode a la GenoM pour creation rapide de fichier .hpp et .cpp (namespace auto, copyright, ...)

* configure

 - ajouter les options pour OpenCV --------- fait
 - take into account the fact tha no more libboostX.so are provided, only libboostX-mt.so
 - look for the boost .pc file?

* module kernel

  - rendre les exceptions plus robustes, heritage std::exception
  - un peu plus de doc

* module "image"

  - structures de base image
  - visualisateur basique en tk en attendant le developpement du serveur
  - fonctions d'affichage (dialogue avec le serveur d'affichage)

* module filter, slam

  - a porter

* documentation

  - page "jafar module", avec utilisation des options du configure pour boost et OpenCV
  - page "Compilation OpenCV sur MacOS X"

* jafarsh

  - un shell jafar ?
  - redefinir les terminate_handler pour recuperer les segfault et
autres bus error

* Subversion

  - script pour aider a l'ajout d'un module sous svn -------- fait
  - method http d'acces au repository

* Bugzilla

  - a definir

* robotpkg

  - choose a stable version per module for packaging	
  - create a release target for the modules
  - do create a category in the doxyfile to be used by release target?
