#!/bin/sh

set -e 
echo "Jafar backbone..."
cd "${JAFAR_DIR}"
git pull --rebase

cd "modules"

for m in $(ls | grep -v CMakeLists.txt); do
    echo ""
    echo "Module ${m}..."
    cd "${m}"
	git pull --rebase
    cd ".."
done


